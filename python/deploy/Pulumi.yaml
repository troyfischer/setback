name: setback-deployment
description: Setback game server deployment on DigitalOcean
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: digitalocean-python
